# Tasks — evidence slice

- [x] T001 Read current runtime, existing research and all generation callers;
  distinguish provider-account scope from bearer identity.
- [x] T002 Write the bounded report in isolated `Notes-1818-mimo-prospective`;
  keep the native-wait/client track separate.
- [x] T003 Add real-HTTP concurrent tests with event-held streams, virtual 8s
  pacing and fixture-port-only networking; establish six raw RED contracts.
- [x] T004 Add three positive controls and a normal artifact test requiring
  exactly six named budget failures (no skip/xfail markers); repeat raw RED in
  20 processes and run full pytest/Ruff.
- [x] T005 Preserve raw results in `evidence/`; propose the minimum opt-in shape
  and document cost/scope/restart blockers instead of inventing enforcement.
- [ ] T006 Deliver source and vault PRs; verify remote head and CI state.

Production implementation and deployment are **not completed or included**.
The six RED spec assertions are explicit unfinished feature contracts, not a
claim that a configured budget was enforced or that provider 429s were solved.
